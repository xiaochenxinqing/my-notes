# 程序在服务器环境执行脚本时的注意事项

1避避免nohup 调用nohup 会无法执行

2/source /etc/profile 可能只对当前脚本能好用, 这个脚本调用的下个脚本中,可能即便写了source /etc/profile 也不好用 可能需要手动指定变量(如java路径)

出现一个怪现象  java进行调用更新脚本,脚本里有通过pdcaatool  restart 这种方式重启进程, ,但结果是可以杀 进程 但是起不来 ???

pdcaatool stop 
