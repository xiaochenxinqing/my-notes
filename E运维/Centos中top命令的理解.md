# Centos中top命令的理解

%CPU 是指某进程占单个逻辑核心数的比例, 比如有6核心12线程(逻辑核) , 则总的值是1200%而不是100%  ,按下1 可以看到占用每个逻辑核心的情况

%MEM是指的某进程占物理内存的占比  注意这个值并不一定准确 有可能运行一段时间就变的很大,看起来占内存很多,这时不用担心无内存可以用,     最终看可用内存还是得看available显示的值
