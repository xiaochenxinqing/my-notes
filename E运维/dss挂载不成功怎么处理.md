### 2️⃣ 创建物理卷（将 `/dev/sda4` 用作 LVM）

```
bash


复制编辑
pvcreate /dev/sda4
```

------

### 3️⃣ 加入到现有卷组 `vg1753751104`

```
bash


复制编辑
vgextend vg1753751104 /dev/sda4
```

------

### 4️⃣ 创建新的逻辑卷（例如，给 `/data` 分配 50G）

```
bash


复制编辑
lvcreate -l 100%FREE -n lv_data vg1753751104
```

> `lv_data` 是逻辑卷名称，你可以自行命名。

------

### 5️⃣ 格式化逻辑卷为 ext4

```
bash


复制编辑
mkfs.ext4 /dev/vg1753751104/lv_data
```

------

### 6️⃣ 挂载临时路径，迁移数据

```
bash复制编辑mkdir /mnt/lv_data
mount /dev/vg1753751104/lv_data /mnt/lv_data
cp -a /data/* /mnt/lv_data/
```

> 使用 `-a` 保留权限、软链等

------

### 7️⃣ 替换原来的 `/data` 目录

```
bash复制编辑mv /data /data.bak          # 保险起见，不直接删除
mkdir /data
umount /mnt/lv_data
mount /dev/vg1753751104/lv_data /data
```

------

### 8️⃣ 验证数据

```
bash复制编辑ls /data
# 应该能看到原来的内容
```

------

### 9️⃣ 设置开机自动挂载（写入 /etc/fstab）

编辑 `/etc/fstab`：

```
bash


复制编辑
nano /etc/fstab
```

添加一行：

```
bash


复制编辑
/dev/vg1753751104/lv_data   /data   ext4    defaults    0 2
```

------

### 🔟 清理旧目录（可选）

确认没问题后可以删掉：

```
bash


复制编辑
rm -rf /data.bak
```

------

## ✅ 最终效果

```
bash复制编辑df -h
# 应该能看到：
# /dev/mapper/vg1753751104-lv_data    50G    XG    YG   Z%   /data
```

## 